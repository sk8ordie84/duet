# Post-challenge backlog

Ideas deliberately NOT implemented before the deadline (polish > features).

- Keyboard accessibility architecture: full keyboard seating (arrow-key seat moves,
  focusable seats/tables), ARIA roles for the board.
- Visible in-UI feedback when the agent's move is refused by pin-law (today the
  refusal lives in the tool response only).
- Shareable read-only plan links (state snapshot behind a short URL).
- Multiple saved events / plan history.
- Table shapes beyond round (rect/banquet rows) in the renderer.
- Seat-level adjacency constraints ("next to", "opposite").
- ChatGPT Sites hosting variant.
- i18n (Turkish first).
